# Copy this to client/.env for Vercel production
# The client app appends /auth/login, /dashboard/summary, etc. automatically.
VITE_API_URL=https://your-render-backend-url.onrender.com/api
# Optional: add app info if needed
# VITE_APP_NAME=Attendance System
